# App icons

- `app_icon.png`: 1024×1024 app icon, derived from `design/logo/source-flat.png` by `scripts/make-icons.sh`
- `mark.png`, `mark-dark.png`: the mark for the light and dark theme

Regenerate platform icons with `dart run flutter_launcher_icons` from `app/`.
